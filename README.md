# My Nix based dotfiles

## Installation

1. Clone the repo into `$HOME/.dotfiles`
2. `cd` into it and run `nix run . --switch --flake .`
3. After the first run you can simply use the `update` alias.
